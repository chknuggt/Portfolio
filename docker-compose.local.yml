services:
  nginx:
    ports:
      - "8080:80"
    volumes:
      - ./docker/nginx/local.conf:/etc/nginx/conf.d/default.conf:ro
    environment:
      - NGINX_ENVSUBST_SKIP=true
  api:
    environment:
      CORS_ORIGINS: "http://localhost:8080"
